<h1>Art Galleries Navigator</h1>

  <% @galleries.each do |gallery| %>
    <h3><%= link_to gallery.title, gallery_path(gallery) %> <%= "(#{gallery.city})" %></h3>
    <ul>
      <% gallery.artists.uniq.each do |artist| %>
        <li><%= artist.name %></li>
      <% end %>
    </ul>
  <% end %>
